import { defineConfig, devices } from '@playwright/test';
export default defineConfig({testDir:'tests/e2e',fullyParallel:true,retries:0,reporter:'line',use:{baseURL:'http://localhost:3001',trace:'retain-on-failure'},projects:[{name:'desktop',use:{...devices['Desktop Chrome']}},{name:'mobile',use:{...devices['iPhone 13'],browserName:'chromium'}}],webServer:{command:'(test -f dist/standalone/server.js || npm run build) && PORT=3001 SITE_URL=http://localhost:3001 DATABASE_PATH=:memory: SESSION_SECRET=playwright-online-secret-at-least-32-characters ONLINE_MATCHING_ENABLED=1 node dist/standalone/server.js',url:'http://localhost:3001',reuseExistingServer:false,timeout:120000}});
